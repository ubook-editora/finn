my_enum = enum {
    hien;
    a;
    b;
}

#b_record = record {
#    b: string;
#}

my_record = record {
    bo: list<my_enum>;
}

hello = interface +c {
    static create(): hello;

#    hlll();

#    say_hello(e: my_enum): my_record;

    say_hi(): my_enum;
}