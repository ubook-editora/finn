my_enum = enum {
    hien;
    a;
    b;
}
my_record = record {
    id: i32;
    e: my_enum;
    resting: optional<i32>;
}

hello = interface +c {
    static create(): hello;

    hlll();

    say_hello(e: my_enum): my_record;

    say_hi(): my_enum;
}